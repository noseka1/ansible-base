kube_lab:
  children:
    kube_master:
      hosts:
        kube1:
          ansible_host: kube1.lab.example.com
          ansible_user: ansible
    kube_worker:
      hosts:
        kube2:
          ansible_host: kube2.lab.example.com
          ansible_user: ansible
