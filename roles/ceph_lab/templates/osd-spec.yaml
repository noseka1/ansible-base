---
service_type: osd
placement:
  hosts:
{% for host in groups.ceph_lab %}
    - {{ hostvars[host]['ansible_host'] }}
{% endfor %}
data_devices:
  paths: {{ cephlab_osd_data_devices_paths | to_yaml }}
