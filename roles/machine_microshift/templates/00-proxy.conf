[Service]
Environment=NO_PROXY="{{ machine_microshift_crio_proxy[ansible_domain].no_proxy }}"
Environment=HTTP_PROXY="{{ machine_microshift_crio_proxy[ansible_domain].http_proxy }}"
Environment=HTTPS_PROXY="{{ machine_microshift_crio_proxy[ansible_domain].https_proxy }}"
