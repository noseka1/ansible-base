[Service]
Environment=NO_PROXY="{{ machine_microshift_http_proxy[ansible_facts.domain].no_proxy }}"
Environment=HTTP_PROXY="{{ machine_microshift_http_proxy[ansible_facts.domain].http_proxy }}"
Environment=HTTPS_PROXY="{{ machine_microshift_http_proxy[ansible_facts.domain].https_proxy }}"
