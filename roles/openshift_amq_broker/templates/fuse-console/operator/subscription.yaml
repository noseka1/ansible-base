apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: fuse-console
  namespace: amq-broker
spec:
  channel: {{ fuseconsole_operator_subscription_channel | default('""', true) }}
  installPlanApproval: Manual
  name: fuse-console
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  channel: {{ fuseconsole_operator_subscription_starting_csv | default('""', true) }}
