apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaTopic
metadata:
  name: {{ item }}
  namespace: {{ amqstreams_topics_namespace }}
  labels:
    strimzi.io/cluster: my-cluster
spec:
  partitions: 1
  replicas: {{ amqstreams_topics_replicas }}
