apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ artifactory_oss_name }}
  namespace: {{ artifactory_oss_namespace }}
spec:
  port:
    targetPort: http
  tls:
    termination: edge
  to:
    kind: Service
    name: {{ artifactory_oss_name }}-artifactory-nginx
