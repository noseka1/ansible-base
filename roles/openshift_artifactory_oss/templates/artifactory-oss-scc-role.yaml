apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ artifactory_oss_name }}-scc
  namespace: {{ artifactory_oss_namespace }}
rules:
- apiGroups:
  - security.openshift.io
  resourceNames:
  - anyuid
  resources:
  - securitycontextconstraints
  verbs:
  - use
