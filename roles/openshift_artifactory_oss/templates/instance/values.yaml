artifactory-oss:
  artifactory:
    nginx:
      service:
        type: ClusterIP
    postgresql:
      auth:
        password: '{{ generic_user_password }}'
      global:
        compatibility:
          openshift:
            adaptSecurityContext: force
