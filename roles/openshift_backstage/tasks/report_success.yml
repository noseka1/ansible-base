- set_fact:
    msg: |
      Backstage installed successfully.

      You can browse to Backstage dashboard:
      https://{{ backstage_name }}-{{ backstage_namespace }}.apps.{{ openshift_common_cluster_domain }}

- debug: msg="{{ msg.split('\n') }}"
