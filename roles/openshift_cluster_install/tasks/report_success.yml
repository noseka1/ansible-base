- name: Report success
  ansible.builtin.set_fact:
    msg: |
      OpenShift installation complete.

      You can check the cluster status using 'KUBECONFIG={{ osclusterinstall_info.config_dir }}/auth/kubeconfig oc get co'

- ansible.builtin.debug:
    msg: "{{ msg.split('\n') }}"
