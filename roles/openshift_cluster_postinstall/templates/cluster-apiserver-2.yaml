apiVersion: config.openshift.io/v1
kind: APIServer
metadata:
  name: cluster
spec:
  servingCerts:
    namedCertificates:
    - names:
      - {{ openshift_cluster_postinstall_endpoint_certs_info.api.domain }}
      servingCertificate:
        name: api-cert
