apiVersion: v1
kind: ConfigMap
metadata:
  name: user-ca-bundle
  namespace: openshift-config
data:
  ca-bundle.crt: |
    {{ openshift_cluster_postinstall_trusted_ca_bundle[openshift_common_cluster_domain] | indent(4) }}
