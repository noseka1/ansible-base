apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: terraform
spec:
  package: crossplane/provider-terraform:{{ crossplane_terraform_provider_version }}
  controllerConfigRef:
    name: terraform
