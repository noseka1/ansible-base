apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: {{ custom_metrics_operator_subscription_name }}
  namespace: {{ custom_metrics_operator_subscription_namespace }}
spec:
  channel: {{ custom_metrics_operator_subscription_channel }}
  installPlanApproval: {{ custom_metrics_operator_subscription_approval }}
  name: openshift-custom-metrics-autoscaler-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
