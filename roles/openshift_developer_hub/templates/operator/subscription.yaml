apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: rhdh
  namespace: rhdh-operator
spec:
  channel: {{ rhdh_operator_subscription_channel | default('""', true) }}
  installPlanApproval: Manual
  name: rhdh
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  channel: {{ rhdh_operator_subscription_starting_csv | default('""', true) }}
