apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: bpfman
  namespace: bpfman
spec:
  channel: {{ ebpfman_operator_subscription_channel | default('""', true) }}
  installPlanApproval: Manual
  name: bpfman-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  channel: {{ ebpfman_operator_subscription_starting_csv | default('""', true) }}
