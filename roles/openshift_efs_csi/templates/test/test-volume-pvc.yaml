apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-volume
  namespace: {{ efs_operator_subscription_namespace }}
spec:
  storageClassName: efs-csi
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
