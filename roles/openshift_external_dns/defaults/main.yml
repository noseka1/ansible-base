external_dns_subscription_namespace: external-dns-operator
external_dns_subscription_name: external-dns-operator

external_dns_instance_namespace: external-dns
external_dns_instance_name: external-dns

external_dns_aws_access_key_id: '{{ undef(hint="You must specify AWS key id") }}'
external_dns_aws_secret_access_key: '{{ undef(hint="You must specify AWS secret access key") }}'

external_dns_domain_name: myzonedomain.example.com
