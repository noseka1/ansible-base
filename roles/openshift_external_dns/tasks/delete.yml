- name: Delete namespace {{ external_dns_instance_namespace }}
  kubernetes.core.k8s:
    template: external-dns-ns.yaml
    state: absent
    wait: true

- name: Delete External DNS operator
  kubernetes.core.k8s:
    template: external-dns-operator-ns.yaml
    state: absent
    wait: true
