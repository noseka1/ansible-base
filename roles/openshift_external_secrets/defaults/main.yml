external_secrets_subscription_namespace: openshift-operators
external_secrets_subscription_name: external-secrets-operator
external_secrets_subscription_channel: stable
external_secrets_subscription_approval: Automatic

external_secrets_instance_namespace: external-secrets
external_secrets_instance_name: external-secrets

# Trusted CA certificate for HashiCorp Vault API endpoint
external_secrets_hvault_ca_bundle: ''

# OIDC-enabled cluster
# true - when ROSA or OCP is configured to use OIDC provider
# false - when ROSA or OCP is NOT configured to use OIDC provider
external_secrets_oidc_enabled: false

# Secrets manager
# hvault - HashiCorp Vault
# aws - AWS Secrets Manager
external_secrets_secrets_manager_type: hvault

external_secrets_aws_region: ''
external_secrets_aws_access_key_id: ''
external_secrets_aws_secret_access_key: ''
external_secrets_aws_role: 'arn:aws:iam::00000000000:role/talk-to-secret-manager'
