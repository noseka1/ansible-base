kind: OperatorConfig
apiVersion: operator.external-secrets.io/v1alpha1
metadata:
  name: {{ extsecrets_instance_name }}
  namespace: {{ extsecrets_instance_namespace }}
spec:
  prometheus:
    enabled: true
    service:
      port: 8080
