apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: {{ extsecrets_operator_subscription_name }}
  namespace: {{ extsecrets_operator_namespace }}
spec:
  channel: {{ extsecrets_operator_subscription_channel | default('""', true) }}
  installPlanApproval: Manual
  name: external-secrets-operator
  source: community-operators
  sourceNamespace: openshift-marketplace
  channel: {{ extsecrets_operator_subscription_starting_csv | default('""', true) }}
