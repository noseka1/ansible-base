apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: gitlab-system
  namespace: {{ gitlab_operator_subscription_namespace }}
spec:
  targetNamespaces:
  - {{ gitlab_operator_subscription_namespace }}
