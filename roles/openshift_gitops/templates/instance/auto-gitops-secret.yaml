{% if gitops_autogitops_enabled %}
apiVersion: v1
kind: Secret
metadata:
  name: auto-gitops
  namespace: {{ gitops_instance_namespace }}
data:
  password: {{ autogitops_git_password | b64encode }}
  username: {{ autogitops_git_username | b64encode }}
{% endif %}
