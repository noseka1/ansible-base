apiVersion: v1
kind: Secret
metadata:
  name: {{ argocd_instance_name }}-cluster
  namespace: {{ argocd_instance_namespace }}
stringData:
  oidc.keycloak.clientSecret: '{{ client_secret_result.json.value }}'
type: Opaque
