apiVersion: v1
kind: Secret
metadata:
  annotations:
    kubernetes.io/service-account.name: grafana-sa
  name: grafana-sa-token-static
  namespace: {{ grafana_instance_namespace }}
type: kubernetes.io/service-account-token
