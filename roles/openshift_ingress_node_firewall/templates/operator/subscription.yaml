apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: ingress-node-firewall
  namespace: openshift-ingress-node-firewall
spec:
{% if ingressnodefw_subscription_channel | length > 0 %}
  channel: {{ ingressnodefw_subscription_channel }}
{% endif %}
  installPlanApproval: Manual
  name: ingress-node-firewall
  source: redhat-operators
  sourceNamespace: openshift-marketplace
{% if ingressnodefw_subscription_starting_csv | length > 0 %}
  startingCSV: {{ ingressnodefw_subscription_starting_csv }}
{% endif %}
