apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: {{ openshift_logging_subscription_name }}
  namespace: {{ openshift_logging_namespace }}
spec:
  channel: {{ openshift_logging_subscription_channel }}
  installPlanApproval: {{ openshift_logging_subscription_approval }}
  name: cluster-logging
  source: redhat-operators
  sourceNamespace: openshift-marketplace
