apiVersion: v1
kind: Secret
metadata:
  name: logging-loki-s3
  namespace: openshift-logging
stringData:
  access_key_id: admin
  access_key_secret: '{{ generic_user_password }}'
  bucketnames: mybucket-1
  endpoint: http://minio:9000
