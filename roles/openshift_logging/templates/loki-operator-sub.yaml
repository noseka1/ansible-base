apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: loki-operator
  namespace: openshift-operators-redhat
spec:
  channel: stable
  installPlanApproval: {{ loki_subscription_approval }}
  name: loki-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
