apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: loki-operator
  namespace: {{ loki_operator_namespace }}
spec:
  channel: {{ loki_operator_subscription_channel | default('""', true) }}
  installPlanApproval: Manual
  name: loki-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  startingCSV: {{ loki_operator_subscription_starting_csv | default('""', true) }}
