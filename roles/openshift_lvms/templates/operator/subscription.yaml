apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: lvms-operator
  namespace: {{ lvms_namespace }}
spec:
  channel: {{ lvms_operator_subscription_channel | default('""', true) }}
  installPlanApproval: Manual
  name: lvms-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  channel: {{ lvms_operator_subscription_starting_csv | default('""', true) }}
