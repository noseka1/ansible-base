apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: mtc-operator
  namespace: openshift-migration
spec:
  channel: {{ mtc_operator_subscription_channel | default('""', true) }}
  installPlanApproval: Manual
  name: mtc-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  startingCSV: {{ mtc_operator_subscription_starting_csv | default('""', true) }}
