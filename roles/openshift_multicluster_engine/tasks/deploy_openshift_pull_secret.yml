- name: Retrieve OpenShift pull secret
  kubernetes.core.k8s_info:
    api_version: v1
    kind: Secret
    name: pull-secret
    namespace: openshift-config
  register: openshift_pull_secret

- name: Deploy pull secret
  kubernetes.core.k8s:
    template: instance/pull-secret-secret.yaml
