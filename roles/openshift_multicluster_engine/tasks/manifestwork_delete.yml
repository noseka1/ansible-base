- name: Delete test-hello-work manifestwork
  kubernetes.core.k8s:
    template: manifestwork/test-hello-work-manifestwork.yaml
    wait: true
    state: absent
  when: >-
    'work.open-cluster-management.io' in lookup('kubernetes.core.k8s', cluster_info='api_groups')
