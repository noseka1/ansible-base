apiVersion: v1
kind: ConfigMap
metadata:
  name: assisted-image-service-trusted-ca
  namespace: multicluster-engine
data:
{% if mce_os_image_trusted_ca | length > 0 %}
  tls.crt: |
    {{ mce_os_image_trusted_ca | indent(4) }}
{% else %}
  tls.crt: ''
{% endif %}
