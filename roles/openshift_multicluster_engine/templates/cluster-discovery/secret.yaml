apiVersion: v1
kind: Secret
metadata:
  labels:
    cluster.open-cluster-management.io/credentials: ""
    cluster.open-cluster-management.io/type: rhocm
  name: ocm-api-token
  namespace: discovered-clusters
stringData:
  auth_method: offline-token
  ocmAPIToken: '{{ redhat_ocm_api_token }}'
