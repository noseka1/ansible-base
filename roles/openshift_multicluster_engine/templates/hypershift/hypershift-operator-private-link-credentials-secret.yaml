apiVersion: v1
kind: Secret
metadata:
  labels:
    cluster.open-cluster-management.io/backup: ""
  name: hypershift-operator-private-link-credentials
  namespace: local-cluster
stringData:
  aws-access-key-id: {{ mce_hypershift_aws.aws_access_key_id }}
  aws-secret-access-key: {{ mce_hypershift_aws.aws_secret_access_key }}
  region: {{ mce_hypershift_aws.aws_region }}
