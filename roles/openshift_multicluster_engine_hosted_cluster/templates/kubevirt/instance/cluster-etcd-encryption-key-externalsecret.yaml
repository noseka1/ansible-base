apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ mcehostedcluster_kubevirt.cluster_name }}-etcd-encryption-key
  namespace: {{ mcehostedcluster_kubevirt.cluster_name }}-hosted
spec:
  secretStoreRef:
    name: autogitops
    kind: ClusterSecretStore
  dataFrom:
  - extract:
      conversionStrategy: Default
      decodingStrategy: None
      metadataPolicy: None
      key: '{{ oscommon_cluster_domain }}/hosted-cluster/kubevirt/{{ mcehostedcluster_kubevirt.cluster_name }}-etcd-encryption-key'
