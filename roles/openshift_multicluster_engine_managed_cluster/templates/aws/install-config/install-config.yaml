apiVersion: v1
metadata:
  name: {{ mcemanagedcluster_info.cluster_name }}
baseDomain: {{ mcemanagedcluster_info.base_domain }}
controlPlane:
  hyperthreading: Enabled
  name: master
  replicas: {{ mcemanagedcluster_info.control_plane_replicas }}
  platform:
    aws:
      rootVolume:
        iops: 4000
        size: 100
        type: io1
      type: m6i.2xlarge
compute:
- hyperthreading: Enabled
  name: worker
  replicas: {{ mcemanagedcluster_info.data_plane_replicas }}
  platform:
    aws:
      rootVolume:
        iops: 2000
        size: 100
        type: io1
      type: m6i.xlarge
networking:
  clusterNetwork:
  - cidr: 10.128.0.0/14
    hostPrefix: 23
  machineCIDR: 10.0.0.0/16
  networkType: OVNKubernetes
  serviceNetwork:
  - 172.30.0.0/16
platform:
  aws:
    region: {{ mcemanagedcluster_info.aws_region }}
pullSecret: "" # skip, hive will inject based on its secrets
sshKey: {{ generic_ssh_key_pair.public_key }}
