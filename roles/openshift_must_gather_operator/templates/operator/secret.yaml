apiVersion: v1
kind: Secret
metadata:
  name: case-management-creds
  namespace: {{ mustgather_operator_namespace }}
stringData:
  username: '{{ must_gather_operator_case_management_username }}'
  password: '{{ must_gather_operator_case_management_password }}'
