apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: {{ network_observability_subscription_name }}
  namespace: {{ network_observability_subscription_namespace }}
spec:
  channel: {{ network_observability_subscription_channel }}
  installPlanApproval: {{ network_observability_subscription_approval }}
  name: netobserv-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
