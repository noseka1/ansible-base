apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ nexus_repository_manager_name }}-scc
  namespace: {{ nexus_repository_manager_namespace }}
rules:
- apiGroups:
  - security.openshift.io
  resourceNames:
  - anyuid
  resources:
  - securitycontextconstraints
  verbs:
  - use
