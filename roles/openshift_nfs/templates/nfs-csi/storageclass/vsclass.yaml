apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshotClass
metadata:
  annotations:
    snapshot.storage.kubernetes.io/is-default-class: >-
      {{ (not oscommon_default_volume_snapshot_class or oscommon_default_volume_snapshot_class == 'nfs-csi') | to_json }}
  labels:
    # Enable creating snapshots using Velero
    velero.io/csi-volumesnapshot-class: 'true'
  name: nfs-csi
driver: nfs.csi.k8s.io
# Deletion policy must be set to Retain or Velero backup using data mover fails
deletionPolicy: Retain
