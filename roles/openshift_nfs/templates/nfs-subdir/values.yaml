nfs:
  server: '{{ rook_nfs_service.resources.0.spec.clusterIP }}'
  path: '/nfs-backing-volume'
serviceAccount:
  name: '{{ nfs_subdir_service_account }}'
storageClass:
  name: nfs-subdir
  defaultClass: {{ not openshift_common_default_storage_class_exists }}
  archiveOnDelete: false
