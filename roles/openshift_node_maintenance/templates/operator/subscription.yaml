apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: {{ nodemaintenance_operator_subscription_name }}
  namespace: {{ nodemaintenance_operator_namespace }}
spec:
  channel: {{ nodemaintenance_operator_subscription_channel | default('""', true) }}
  installPlanApproval: Manual
  name: node-maintenance-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  channel: {{ nodemaintenance_operator_subscription_starting_csv | default('""', true) }}
