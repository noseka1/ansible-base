- import_role:
    name: openshift_minio
    tasks_from: deploy.yml
  vars:
    minio_namespace: '{{ oadp_subscription_namespace }}'
  when: infrastructure.resources.0.status.platformStatus.type != 'AWS'
