apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: {{ oadp_subscription_name }}
  namespace: {{ oadp_subscription_namespace }}
spec:
  targetNamespaces:
    - {{ oadp_subscription_namespace }}
