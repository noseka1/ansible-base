{% if odf_openshift_storage_namespace_result.resources | length == 0 %}
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: openshift-storage
  namespace: {{ odf_namespace }}
spec:
  targetNamespaces:
  - {{ odf_namespace }}
{% endif %}
