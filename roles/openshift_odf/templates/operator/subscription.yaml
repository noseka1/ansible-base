apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: odf-operator
  namespace: {{ odf_namespace }}
spec:
  channel: {{ odf_operator_subscription_channel | default('""', true) }}
  installPlanApproval: Manual
  name: odf-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  startingCSV: {{ odf_operator_subscription_starting_csv | default('""', true) }}
