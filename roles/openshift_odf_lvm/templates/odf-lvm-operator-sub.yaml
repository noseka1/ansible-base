apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: odf-lvm-operator
  namespace: openshift-storage
spec:
{% if odf_lvm_subscription_channel | length > 0 %}
  channel: {{ odf_lvm_subscription_channel }}
{% endif %}
  installPlanApproval: {{ odf_lvm_subscription_approval }}
  name: odf-lvm-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
