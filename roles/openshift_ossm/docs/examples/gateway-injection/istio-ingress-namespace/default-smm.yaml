apiVersion: maistra.io/v1
kind: ServiceMeshMember
metadata:
  name: default
spec:
  # Register this namespace with the Istio control plane:
  controlPlaneRef:
    name: control-plane
    namespace: istio-system
