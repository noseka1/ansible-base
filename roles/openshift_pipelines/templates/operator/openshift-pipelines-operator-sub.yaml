apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: {{ openshift_pipelines_subscription_name }}
  namespace: {{ openshift_pipelines_subscription_namespace }}
spec:
  channel: {{ openshift_pipelines_subscription_channel }}
  installPlanApproval: {{ openshift_pipelines_subscription_approval }}
  name: openshift-pipelines-operator-rh
  source: redhat-operators
  sourceNamespace: openshift-marketplace
