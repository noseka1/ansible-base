apiVersion: v1
kind: Secret
metadata:
  name: {{ quay_instance_name }}-admin-token
  namespace: {{ quay_instance_namespace }}
stringData:
  access_token: '{{ quay_admin_initialize.json.access_token }}'
