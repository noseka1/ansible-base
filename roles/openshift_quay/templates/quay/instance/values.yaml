storageHostname: minio-api-{{ quay_instance_namespace }}-minio.apps.{{ openshift_common_cluster_domain }}
storagePort: '443'
storageIsSecure: true
storageAccessKey: admin
storageSecretKey: '{{ generic_user_password }}'
storageBucketName: mybucket1

quayReplicas: 1

clairManaged: {{ quay_clair_managed }}
