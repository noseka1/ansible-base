- name: Delete {{ rhacm_managed_cluster.cluster_name }} managed cluster
  kubernetes.core.k8s:
    template:
      - cluster-managedcluster.yaml
      - cluster-ns.yml
    state: absent
    wait: true
    wait_timeout: 1800
