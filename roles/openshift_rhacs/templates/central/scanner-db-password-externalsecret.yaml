apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: scanner-db-password
  namespace: stackrox
spec:
  secretStoreRef:
    name: autogitops
    kind: ClusterSecretStore
  dataFrom:
  - extract:
      conversionStrategy: Default
      decodingStrategy: None
      metadataPolicy: None
      key: '{{ oscommon_cluster_domain }}/rhacs/central/scanner-db-password'
