- ansible.builtin.import_role:
    name: openshift_common
    tasks_from: get_infrastructure.yml

- ansible.builtin.set_fact:
    rhbk_instance_hostname: keycloak-keycloak.apps.{{ oscommon_cluster_domain }}
