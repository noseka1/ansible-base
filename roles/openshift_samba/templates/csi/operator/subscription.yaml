apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: {{ smbcsi_operator_subscription_name }}
  namespace: {{ smbcsi_operator_namespace }}
spec:
  channel: {{ smbcsi_operator_subscription_channel | default('""', true) }}
  installPlanApproval: Manual
  name: smb-csi-driver-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  channel: {{ smbcsi_operator_subscription_starting_csv | default('""', true) }}
