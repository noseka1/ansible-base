- name: Delete standalone Splunk instance namespace
  kubernetes.core.k8s:
    template:
      - instance/splunk-ns.yaml
    wait: true
    state: absent

- name: Delete Splunk operator
  kubernetes.core.k8s:
    template:
      - operator/splunk-sub.yaml
    wait: true
    state: absent
