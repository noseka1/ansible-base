apiVersion: machineconfiguration.openshift.io/v1
kind: MachineConfig
metadata:
  labels:
    machineconfiguration.openshift.io/role: {{ item }}
  name: 62-{{ item }}-network-sriov-igb
spec:
  kernelArguments: {{ osclusterpostinst_kernel_arguments | to_yaml }}
