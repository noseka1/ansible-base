apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: support-log-gather
  namespace: support-log-gather-operator
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100G
