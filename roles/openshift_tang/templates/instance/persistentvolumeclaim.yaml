apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tangserver
  namespace: {{ tang_instance_namespace }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Mi
