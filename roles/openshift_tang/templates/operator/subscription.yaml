apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: tang-operator
  namespace: openshift-operators
spec:
  channel: {{ tang_operator_subscription_channel | default('""', true) }}
  installPlanApproval: Manual
  name: tang-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  startingCSV: {{ tang_operator_subscription_starting_csv | default('""', true) }}
