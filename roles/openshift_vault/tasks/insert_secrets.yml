- import_tasks: insert_secret.yml
  vars:
    secret_name: mysecret
    secret_data:
      generic_user_password: '{{ generic_user_password }}'
      redhat_quay_pull_secret: '{{ redhat_quay_pull_secret }}'
      testkey: testvalue
