apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: {{ vertical_pod_autoscaler_subscription_name }}
  namespace: {{ vertical_pod_autoscaler_subscription_namespace }}
spec:
  targetNamespaces:
  - {{ vertical_pod_autoscaler_instance_namespace }}
  upgradeStrategy: Default
