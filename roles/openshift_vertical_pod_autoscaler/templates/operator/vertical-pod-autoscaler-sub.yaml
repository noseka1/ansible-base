apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: {{ vertical_pod_autoscaler_subscription_name }}
  namespace: {{ vertical_pod_autoscaler_subscription_namespace }}
spec:
  channel: {{ vertical_pod_autoscaler_subscription_channel }}
  installPlanApproval: {{ vertical_pod_autoscaler_subscription_approval }}
  name: vertical-pod-autoscaler
  source: redhat-operators
  sourceNamespace: openshift-marketplace
