kind: ConfigMap
apiVersion: v1
metadata:
  labels:
    config.openshift.io/inject-trusted-cabundle: 'true'
  name: {{ openshift_virtualization_machine_name }}-trusted-ca-bundle
  namespace: {{ openshift_virtualization_machine_namespace }}
