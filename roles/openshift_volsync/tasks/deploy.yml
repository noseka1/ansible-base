- name: Deploy VolSync operator
  kubernetes.core.k8s:
    template: volsync-product-sub.yaml

- import_role:
    name: openshift_common
    tasks_from: wait_for_operator.yml
  vars:
    subscription_name: '{{ volsync_subscription_name }}'
    subscription_namespace: '{{ volsync_subscription_namespace }}'
