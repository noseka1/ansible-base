apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: web-terminal
  namespace: openshift-terminal-operator
spec:
  channel: {{ webterm_operator_subscription_channel | default('""', true) }}
  installPlanApproval: Manual
  name: web-terminal
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  startingCSV: {{ webterm_operator_subscription_starting_csv | default('""', true) }}
