<network>
  <name>{{ network.name }}</name>
  <forward mode='nat'>
    <nat>
      <port start='1024' end='65535'/>
    </nat>
  </forward>
  <bridge name='{{ network.bridge_name }}' stp='on' delay='0'/>
  <mac address='{{ network.bridge_mac }}'/>
  <ip address='{{ network.bridge_ip }}' netmask='{{ network.bridge_netmask }}'>
    <dhcp>
      <range start='{{ network.ip_range_start }}' end='{{ network.ip_range_end }}'/>
{% for key, value in virtual_machines.items() %}
      <host mac='{{ value.mac }}' name='{{ virtual_machines_domain }}.{{ value.name }}' ip='{{ value.ip }}'/>
{% endfor %}
    </dhcp>
  </ip>
  <dns>
    <forwarder addr='{{ virtual_machines.osh_loadbalancer.ip }}'/>
  </dns>
</network>
