openshift_version: 4.6.8
aws_region: us-west-2
cluster_config_dir: '{{ ansible_env["HOME"] }}/cluster-{{ otlc_service_info.guid }}'

epel_repo_url: 'https://dl.fedoraproject.org/pub/epel/epel-release-latest-{{ ansible_distribution_major_version }}.noarch.rpm'
epel_repo_gpg_key_url: '/etc/pki/rpm-gpg/RPM-GPG-KEY-EPEL-{{ ansible_distribution_major_version }}'
epel_repofile_path: '/etc/yum.repos.d/epel.repo'

ocp_availability_zones: [ a, b, c ]
ocp_nodes_per_zone: 2
ocp_nodes_total: '{{ (ocp_availability_zones | length) * ocp_nodes_per_zone }}'
